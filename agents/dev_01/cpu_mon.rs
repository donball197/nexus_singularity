use std::fs; fn main() { let load = fs::read_to_string("/proc/loadavg").unwrap_or("0.0".to_string()); let parts: Vec<&str> = load.split_whitespace().collect(); println!(r#"{{"cpu_load": {}, "status": "nominal"}}"#, parts[0]); }